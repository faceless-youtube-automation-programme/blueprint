# GitHub Pages root export

This is a fully built static export. `index.html` is intentionally at the repository root.

Upload the contents of this folder directly to the root of your GitHub repository. Do not upload the enclosing ZIP file and do not place these files inside another folder.

In GitHub, open Settings → Pages and select Deploy from a branch, choose `main` and `/ (root)`, then save.
